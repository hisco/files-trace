class MessageBase{
    constructor(){
        this.type = 'to be a message in the system'
    }
}
throw new Error(`proof that it's not being required or something wierd like that..`)

module.exports ={
    MessageBase
}